# local-ai

Just messing with local LLMs on an M4 Max MacBook Pro (64GB memory).

## GUI

```bash
pip install PySide6   # one-time, in venv
./run-gui.sh
```

Lists cached models, start/stop servers on any port, delete models. Uses PySide6 (pip-installable, no system Tkinter needed).
